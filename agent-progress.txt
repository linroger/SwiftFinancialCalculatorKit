Project Overview
FinancialCalculatorKit is a native macOS SwiftUI + SwiftData financial toolkit with multiple calculator surfaces, charting, and saved calculation models. The repository entered this session without a working long-running-agent harness, without meaningful tests, and with an invalid Git `HEAD`, so the first priority is to establish continuity and deterministic build/run workflows before deep feature edits.

Session Log

- 2026-04-13 20:09:17 UTC
  Branch: invalid HEAD / no readable commit history
  Feature or bug worked on: project discovery, architecture mapping, and harness initialization
  Files touched: `handoff.md`, `feature_list.json`, `agent-progress.txt`, `PLANS.md`, `init.sh`, `script/build_and_run.sh`, `.codex/environments/environment.toml`
  Summary:
  Read the project documentation and inspected the app shell, view model, utilities, services, calculation models, major calculator views, dashboard, charts, and shared components. Confirmed the codebase compiles with SwiftPM and Xcode build, but found major product gaps: monolithic root views, misleading dashboard “Live Data,” no-op preference persistence, incomplete recent-calculation deletion, ignored expression variables, placeholder options analysis text, and a heavily polluted Xcode project source graph.
  Tests and verification:
  Ran `swift build` successfully.
  Ran `xcodebuild -list -project FinancialCalculatorKit.xcodeproj` successfully.
  Ran `xcodebuild -project FinancialCalculatorKit.xcodeproj -scheme FinancialCalculatorKit -destination 'platform=macOS' build` successfully.
  Ran `xcodebuild ... test` concurrently with build once and hit a locked build database; this is a workflow issue that needs serialized test execution.
  Added `init.sh` and ran it successfully.
  Added `script/build_and_run.sh` and `./script/build_and_run.sh --verify` failed with duplicate generated output errors from the Xcode project, which is now the clearest concrete blocker.
  Result:
  Harness established; implementation slice not started yet.
  Remaining follow-ups / known issues:
  Duplicate and unrelated PBX source entries can escalate into deterministic build failures.
  Help, preferences, and dashboard promise capabilities that are not fully implemented.
  Persistence flows are incomplete across calculation types.
  Financial math needs verification beyond successful compilation.

Next Steps
- Clean or regenerate the Xcode project source phases so `script/build_and_run.sh --verify` succeeds reliably.
- Stabilize the app shell before touching individual calculator features.
- Re-run serialized build and test commands after project hygiene fixes, then start the shared persistence slice.

- 2026-04-13 20:43:07 UTC
  Branch: invalid HEAD / no readable commit history
  Feature or bug worked on: `feature_app_shell_navigation`
  Files touched: `FinancialCalculatorKit.xcodeproj/project.pbxproj`, `FinancialCalculatorKit/ViewModels/MainViewModel.swift`, `FinancialCalculatorKit/ContentView.swift`, `FinancialCalculatorKit/Views/DashboardView.swift`, `FinancialCalculatorKitTests/FinancialCalculatorKitTests.swift`, `FinancialCalculatorKitUITests/FinancialCalculatorKitUITests.swift`, `FinancialCalculatorKitUITests/FinancialCalculatorKitUITestsLaunchTests.swift`, `feature_list.json`, `handoff.md`, `agent-progress.txt`
  Summary:
  Cleaned the corrupted Xcode project source phases so the scripted build-and-launch path stopped producing duplicate generated outputs. Refactored the app shell to use a single sheet-routing enum in `MainViewModel`, wired toolbar and dashboard entry points through shared view-model actions, removed fragile sheet state fan-out, and added macOS shell smoke coverage for keyboard shortcuts and sidebar calculator navigation.
  Tests and verification:
  Ran `plutil -lint FinancialCalculatorKit.xcodeproj/project.pbxproj` successfully.
  Ran `xcodebuild -project FinancialCalculatorKit.xcodeproj -scheme FinancialCalculatorKit -destination 'platform=macOS' -derivedDataPath .derivedData test` successfully.
  Verified unit tests `createNewCalculationPresentsCalculatorPicker()`, `sheetRoutingIsExclusive()`, and `openCalculatorAndDashboardResetSelection()` passed.
  Verified UI tests opened New Calculation with `⌘N`, Preferences with `⌘,`, Help with `⌘⇧/`, and navigated to Loan Calculator successfully.
  Ran `./script/build_and_run.sh --verify` successfully and observed `FinancialCalculatorKit launched successfully.`
  Result:
  `feature_app_shell_navigation` is now passing and the app has a deterministic local build, launch, and shell-navigation verification path.
  Remaining follow-ups / known issues:
  Git refs are still broken, so commit/push work remains blocked.
  Dashboard truthfulness still needs cleanup, especially around mock-versus-live positioning.
  Preference persistence, recents/favorites/delete flows, and calculator math validation are still incomplete.

Next Steps
- Repair dashboard content and quick actions so the landing screen only promises what the app actually implements.
- Implement real preference persistence plus recent/favorite/delete flows across saved calculations.
- Keep `./script/build_and_run.sh --verify` and the shell UI smoke green while moving into the next slice.

- 2026-08-27 21:25 UTC
  Branch: main (fresh isolated git repository initialized in the project root; the enclosing home-directory repo has a broken HEAD and was left untouched)
  Feature or bug worked on: full audit-and-repair pass across engine, models, views, components, and project file
  Files touched: CalculationEngine.swift, TimeValueAnalytics.swift, ExchangeRateService.swift, all 9 calculation models, all 9 calculator views, OptionsAnalyticsSheets.swift, ContentView.swift, DashboardView.swift, MainViewModel.swift, Formatters.swift, Currency.swift, InputFieldView.swift, ResultDisplayView.swift, FinancialChartView.swift, FinancialStyles.swift, new Utilities/CalculationExporter.swift, project.pbxproj, FinancialCalculatorKitTests.swift, FinancialCalculatorKitUITests.swift; deleted unused Item.swift
  Summary:
  Ran three parallel code audits (~90 findings), fixed all critical/major defects: Black-Scholes CDF missing /sqrt(2), loan payment at 100x rate, TVM solver sign conventions and unsolvable-target handling, 1...0 range crashes, IRR/YTM bracket validation, MIRR implementation, expression variables ignored, identity temperature conversion, fabricated currency rates, no-op preferences/save/delete/export flows, MACRS and declining-balance schedule correctness, dashboard honesty and completeness, and pbxproj corruption (missing unit-test product reference, duplicate compile entries).
  Tests and verification:
  xcodebuild build succeeds with zero project warnings (DEVELOPER_DIR=Xcode-beta.app, -skipMacroValidation required on this machine).
  xcodebuild test (unit target): 25/25 tests pass, including textbook Black-Scholes 10.4506 ATM value, $1,798.65 mortgage payment with $347,514.57 total interest, IRR 23.375% and MIRR 18.29% hand-checks, TVM solver round-trips, bond YTM/price round-trip, zero-rate bond pricing, DDB-to-SL crossover reaching salvage, fractional-life and short-maturity crash regressions, expression-variable resolution, and preferences UserDefaults round-trip.
  UI tests: hardened sheet-dismissal waits; this headless CLI session intermittently loses UI-testing authorization ("Not authorized for performing UI testing actions"), so UI smoke needs a GUI session to confirm.
  Result:
  Engine/model correctness repaired and regression-locked. 9 of 15 features now passing in feature_list.json.
  Remaining follow-ups / known issues:
  Currency and unit converter flows verified by review and build only (no automated tests); export/import feature needs an interactive verification of the NSSavePanel flow; the new planning tool (feature_new_retirement_or_planning_tool) is still unbuilt; UI tests need a GUI session.

- 2026-08-27 22:05 UTC
  Branch: main (isolated project repository)
  Feature or bug worked on: exhaustive re-verification pass + feature_new_retirement_or_planning_tool
  Files touched: RetirementPlanCalculation.swift (new), RetirementPlannerView.swift (new), CalculationType.swift, FinancialCalculatorKitApp.swift, ContentView.swift, DashboardView.swift, CalculationEngine.swift, TimeValueAnalytics.swift, TimeValueCalculation.swift, LoanCalculation.swift, InvestmentCalculation.swift, DepreciationCalculation.swift, all calculator views, UnitConverterView.swift, CurrencyConverterView.swift, CalculationExporter.swift, README.md, tests
  Summary:
  A multi-agent re-audit surfaced 55 candidate defects; 36 were adversarially confirmed and ALL were fixed by hand: amortization-table pagination crash (search while paged), typing-hostile Double-description bindings on term/years fields, stale results resurrected by leftover asyncAfter delays, down-payment leaking into standard loans, bond "No solution" sentinel enabling zero-yield analytics, IRR endpoint-only bracket check missing interior roots, TVM analytics silently truncating horizons past 1200 periods (now full-horizon with plot striding), Int-conversion traps on absurd inputs (clamps + input caps), stale exchange rates surviving base-currency switches (task cancellation + base guard), fabricated 1.0 seed rate, per-currency formatting in the converter, currency pickers hiding the user's own default currency (all 16 now shown everywhere, selection always present), duplicate field labels, dead spinner/state code, MACRS depreciable-base contradiction, payback period missing in combined mode, sensitivity sheet showing IRR-as-dollars, VaR sign convention, fractional-coupon duration/convexity, dashboard currency-conversion blind spot, contradictory sample-market arrow, accessibility labels on icon-only controls, unit-category picker truncation, developer wording in user copy, honest 16-currency and CSV-export claims.
  New feature: Retirement Planner — accumulation projection, growing-annuity required nest egg, sustainable income, extra-savings-needed, depletion-age simulation, balance timeline chart; wired into navigation, schema, sidebar recents (delete/favorite), dashboard stats/activity, help, README (EN+中文).
  Tests and verification:
  35/35 unit tests pass, including 4 new retirement-math tests (closed-form vs brute-force growing annuity, zero-rate arithmetic, shortfall/depletion, sustainable-income self-consistency), temperature conversion fixed-point checks, CSV escaping, and a SwiftData insert/favorite/delete round-trip across the 10-model schema. Exchange-rate provider verified reachable and returning result:success from this machine. Build clean, zero warnings.
  Result:
  feature_list.json: 15 of 15 features passing.

- 2026-08-28 (iterations 1-4, self-paced loop)
  Branch: main → origin/v2
  Features added this session:
  1. Monte Carlo retirement analysis — up to 10k seeded market paths, success probability, p10/p50/p90 fan chart, sustainable income at 90% confidence, depletion age. Runs off the main actor.
  2. Menu-bar commands — Calculators menu, ⌘0 dashboard, ⌘1-9 per calculator, ⌘⇧F favorites. MainViewModel hoisted to the App to make this possible.
  3. Refinance analyzer (Loan Calculator) — payments-made slider derives current balance, break-even chart on closing costs, lifetime cost kept separate from monthly savings, same-payment acceleration scenario.
  4. Debt Payoff Planner (new calculator) — avalanche vs snowball vs minimums-only, equal-budget comparison with minimum rollover, payoff order, three-curve chart, new SwiftData model storing debts as JSON.
  5. Command palette (⌘K) — matches calculators on name/category/description/jargon; testable pure matching function.
  6. Saved-calculation restore — the big one. Save was previously write-only across the whole app; all nine saving calculators now reopen their records with results intact, via a type-scoped pendingLoadID handshake.
  Tests: 43 → 67, all green. Build clean, app launches/quits cleanly each iteration.
  Three test failures during this work were my own wrong assumptions, each corrected against measured behavior rather than by loosening the assertion:
   - volatility does NOT always lower Monte Carlo success (it raises it for a plan that already fails deterministically)
   - milestone order != targeting order in debt payoff (a small balance with a large minimum clears first regardless of strategy)
   - "loan" in the palette matched only one calculator — fixed the keywords, not the test
  Remaining known gaps: UI tests still need a GUI session (macOS automation auth); Currency/Unit converters do not persist to the sidebar the way other calculators do.
